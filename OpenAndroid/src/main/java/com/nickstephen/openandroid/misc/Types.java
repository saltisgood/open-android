package com.nickstephen.openandroid.misc;

/**
 * An enum of the different types used for identifying the components of classes
 */
public enum Types {
    CLASS, INTERFACE, ENUM, ANNOTATION, METHOD, FIELD
}
